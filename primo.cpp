/* The first C program */

#include <stdio.h>

int main (){
	printf("Hello World!\n Bye Bye"); /*funzione presa in prestito dalla libreria stdio, tra parentesi tonde cè quello che deve stampare, ovvero una sequenza di caratteri (stringa).*/
	/*compilatore trasforma in file da eseguire, faremo solo programmi con linea di comanda no mouse ecc. \n sta per new line, \ introduce caratt spec*/
	return 0; /* serve a "chiudere" il programma */
	/* punto e virgola è il terminatore di istruzione, non cè nel main, include e cpommenti pk li non termina l'istruzione. Tra le graffe si dice che è presente un blocco di codice. */
}
