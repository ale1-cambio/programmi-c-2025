#include <stdio.h>

int main(){
	int n,V[20],i,k;
	printf("Inserisci il numero di cui vuoi calcolare i numeri primi compresi tra 2 e n: ");
	scanf("%d", &n);
	for (i=0;i<n;i++){
		V[i]=i+1;
		printf("%d", V[i]);
	}
	for(i=0;i<n;i++){
		
	}
}